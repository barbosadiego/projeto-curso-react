import React from 'react'
import Feed from '../Feed/Feed'

const Home = (props) => {
  return (
    <section className='container mainContainer'>
      <Feed/>
    </section>
  )
}

export default Home
